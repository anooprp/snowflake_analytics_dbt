name: 'snowflake_analytics'
version: '1.0'
config-version: 2

model-paths:
  - models

models:
  snowflake_analytics:
    sfa:
      raw:
        +schema: raw
      staging:
        +schema: stg
      intermediate:
        +schema: int
      marts:
        +schema: core

    test:
      raw:
        +schema: raw
      staging:
        +schema: stg
      intermediate:
        +schema: int
      marts:
        +schema: core
